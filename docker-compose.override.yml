version: '3'

services:
  web:
    volumes:
      - .:/src

  trend:
    volumes:
     - .:/src
    environment:
      - TWITTERSTREAM=${TWITTERSTREAM}
      - REMOVETWITTERFILES=${REMOVETWITTERFILES}

  recommendation:
    volumes:
     - .:/src
